{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GatewayRestartInputs",
  "description": "Input schema for gateway_restart action.",
  "type": "object",
  "required": ["reason"],
  "properties": {
    "reason": {
      "type": "string",
      "minLength": 3,
      "description": "Reason for gateway restart. Must be at least 3 non-whitespace characters and must not be purely numeric. Semantic validation is enforced by plugin validateActionInputs (the authoritative source); this schema provides contract-alignment only."
    }
  },
  "additionalProperties": false
}
