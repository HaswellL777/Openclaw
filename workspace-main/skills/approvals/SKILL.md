# approvals skill

Use approval flow when:
- host runtime configuration will change
- systemd behavior will change
- broker write actions are requested
- Docker host boundary will change
- snapshot / Vault workflow will be affected
