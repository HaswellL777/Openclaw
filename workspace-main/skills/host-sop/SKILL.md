# host-sop skill

When host facts are needed:
1. read `control/SOP.md`
2. prefer SOP over stale memory
3. if runtime config path is relevant, treat `/etc/openclaw/openclaw.json` as authoritative
4. distinguish root snapshot state from `/var/lib/openclaw` runtime state
